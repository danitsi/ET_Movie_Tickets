
import 'package:flutter/material.dart';

class TheatersPage extends StatelessWidget {
  const TheatersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
